# Build artifact - regenerate with: c:/xampp/php/php.exe build-zip.php
# Not with PowerShell's Compress-Archive: it writes backslashes into the zip
# and WordPress then rejects the upload as missing style.css.
antradus-theme.zip
*.zip

# Never commit credentials
pwd.txt
*.pem
*.key
.env
.env.*

# OS / editor junk
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp
*~
.vscode/
.idea/
